project directory structure:

Note: FastAPI has no official project structure - following a sample approach has been presented:

- core      -> main project dir
- crud      ->  database operation functions
                repository classes handle database operation
- db        ->  hold databse models and connection setup 
- router    ->  contain api enfpoints  
- schemas   ->  data validation model 

